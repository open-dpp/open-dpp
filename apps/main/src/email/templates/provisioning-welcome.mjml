<mjml>
  <mj-head>
    {{> emailHead }}
  </mj-head>
  <mj-body background-color="#6BAD87">
    {{> emailHeader }}
    <mj-section padding-bottom="20px" padding-top="10px">
      <mj-column>
        <mj-text align="center" padding="10px 25px" font-size="20px" color="#512d0b"
          ><strong>{{#if firstName}}Hello {{ firstName }},{{else}}Hello,{{/if}}</strong></mj-text
        >
        <mj-text align="center" font-size="18px" font-family="Arial"
          >Your organization {{ organizationName }} has been set up for you on open-dpp.<br />
          Set your password to get started.</mj-text
        >
        <mj-button
          background-color="#6BAD87"
          color="#FFFFFF"
          href="{{ link }}"
          font-family="Arial, sans-serif"
          padding="20px 0 0 0"
          font-weight="bold"
          font-size="16px"
          >Start using open-dpp</mj-button
        >
        <mj-text align="center" font-size="18px" font-family="Arial" padding-top="20px"
          >The link is valid for one hour. If it has expired, use &ldquo;Forgot password&rdquo; on
          the sign-in page to request a new one.</mj-text
        >
      </mj-column>
    </mj-section>
  </mj-body>
</mjml>
